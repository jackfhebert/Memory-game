// Single source of truth for the version shown on the player-select screen
// (js/players.js). Bump per CLAUDE.md's versioning policy: patch for small
// fixes, minor for features, major left at 1 until something major changes.
export const APP_VERSION = "1.1.0";
